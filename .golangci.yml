version: "2"

linters:
  disable:
    - errcheck  # Too many false positives from fatih/color and standard defer-close patterns

issues:
  exclude-dirs:
    - web
    - dist

  exclude-files:
    - ".*/vuln_samples/.*\\.go"
